# OPENAI_API_KEY: Required for embeddings and chat (gpt-4o-mini + text-embedding-3-small).
OPENAI_API_KEY="sk-your-openai-api-key"

# Optional JWT secrets (defaults are used if not set)
JWT_ACCESS_SECRET="your-access-secret"
JWT_REFRESH_SECRET="your-refresh-secret"
